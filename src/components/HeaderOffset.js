import React, { Component } from "react";

class HeaderOffset extends Component {
    render(props) {
        return (
            <div id="header-offset"></div>
        );
    }
}

export default HeaderOffset;
